# Base URL of the ScrollWise API.
VITE_API_BASE=http://localhost:8000

# Google Sign-In (optional). Set this to the SAME OAuth client id the API uses
# (GOOGLE_CLIENT_ID). When set, the "Sign in with Google" button appears.
VITE_GOOGLE_CLIENT_ID=
